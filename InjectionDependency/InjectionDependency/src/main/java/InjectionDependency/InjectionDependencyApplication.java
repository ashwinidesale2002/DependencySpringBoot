//Create a spring boot project to implement constructor injection in a Trainer class with subject as the dependency

package InjectionDependency;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class InjectionDependencyApplication {

	public static void main(String[] args) {
		SpringApplication.run(InjectionDependencyApplication.class, args);
	}

}
